{
  "name": "moodyo-mean",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix client && npm run --prefix client build -- --configuration=production",
    "start": "cd server && npm ci && npm start",
    "client:serve": "cd client && ng serve",
    "server:dev": "cd server && npm run dev"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "description": "Mood-based music player with MEAN stack",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.1",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "@angular/cli": "^20.3.7",
    "@angular-devkit/build-angular": "^20.3.7"
  }
}
