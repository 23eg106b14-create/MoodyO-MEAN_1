{
  "name": "moodyo-mean",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npx ng build --configuration production",
    "start": "cd server && npm ci && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "description": "Mood-based music player with MEAN stack"
}
