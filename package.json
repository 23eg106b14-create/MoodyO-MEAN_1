{
  "name": "moodyo-mean",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Installing client dependencies...' && cd client && npm install && echo 'Building Angular app...' && npm run build --prod",
    "start": "echo 'Starting server...' && cd server && npm install && npm start",
    "postinstall": "echo 'Root dependencies installed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@angular/cli": "^20.3.6"
  },
  "description": "Mood-based music player with MEAN stack"
}
